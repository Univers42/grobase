# App Scripts

The app service is built and run through Docker Compose. Operational scripts are exposed from root `scripts/` and service-specific deployment scripts live under `infrastructure/services/fly/scripts/`.