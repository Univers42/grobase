# GENERATED by scripts/deploy/gen-deploy.py from the Makefile manifest + docker-compose.yml.
# Do not edit by hand — run `make deploy-gen`.
edition: lean
global:
  containerSecurityContext:
    allowPrivilegeEscalation: false
    capabilities:
      drop:
      - ALL
    privileged: false
  imagePullPolicy: IfNotPresent
  imageRegistry: ''
  imageTag: latest
  nameOverride: mini-baas
  podSecurityContext:
    fsGroup: 10001
    runAsGroup: 10001
    runAsNonRoot: true
    runAsUser: 10001
    seccompProfile:
      type: RuntimeDefault
namespace: ''
namespaceDefault: mini-baas
networkPolicy:
  enabled: true
  ingressControllerNamespace: ingress-nginx
  ingressTargets:
  - kong
  - waf
services:
  adapter-registry-go:
    enabled: false
    image: ghcr.io/univers42/grobase-adapter-registry-go:latest
    ports:
    - 3021
    probe:
      exec:
      - /app/app
      - --healthcheck
      failureThreshold: 5
      initialDelaySeconds: 30
      periodSeconds: 30
      timeoutSeconds: 3
    profiles:
    - control-plane
    - go-control-plane
    replicas: 1
    resources:
      limits:
        cpu: 2000m
        memory: 192Mi
      requests:
        cpu: 1000m
        memory: 0Mi
  ai-service:
    enabled: false
    image: ghcr.io/univers42/grobase-ai-service:latest
    ports: []
    probe:
      exec:
      - sh
      - -c
      - wget -qO- http://localhost:3100/health/live || exit 1
      failureThreshold: 5
      initialDelaySeconds: 30
      periodSeconds: 30
      timeoutSeconds: 3
    profiles:
    - analytics
    replicas: 1
    resources:
      limits:
        cpu: 500m
        memory: 256Mi
      requests:
        cpu: 250m
        memory: 0Mi
  analytics-service:
    enabled: false
    image: ghcr.io/univers42/grobase-analytics-service:latest
    ports: []
    probe:
      exec:
      - sh
      - -c
      - wget -qO- http://localhost:3070/health/live || exit 1
      failureThreshold: 5
      initialDelaySeconds: 30
      periodSeconds: 30
      timeoutSeconds: 3
    profiles:
    - analytics
    replicas: 1
    resources:
      limits:
        cpu: 250m
        memory: 128Mi
      requests:
        cpu: 125m
        memory: 0Mi
  cockroach:
    enabled: false
    image: ghcr.io/univers42/grobase-cockroach:latest
    ports:
    - 8080
    - 26257
    probe:
      exec:
      - cockroach
      - sql
      - --insecure
      - --host=127.0.0.1:26257
      - -e
      - SELECT 1
      failureThreshold: 5
      initialDelaySeconds: 20
      periodSeconds: 10
      timeoutSeconds: 5
    profiles:
    - engines-extra
    replicas: 1
    resources:
      limits:
        cpu: 1000m
        memory: 1024Mi
      requests:
        cpu: 500m
        memory: 0Mi
  data-plane-router-rust:
    enabled: false
    image: ghcr.io/univers42/grobase-data-plane-router:latest${DATA_PLANE_IMAGE_SUFFIX:-}
    ports:
    - 4011
    probe:
      exec:
      - /app/data-plane-router
      - --healthcheck
      failureThreshold: 5
      initialDelaySeconds: 30
      periodSeconds: 30
      timeoutSeconds: 3
    profiles:
    - adapter-plane
    - rust-data-plane
    replicas: 1
    resources:
      limits:
        cpu: 250m
        memory: 96Mi
      requests:
        cpu: 125m
        memory: 0Mi
  db-bootstrap:
    enabled: true
    image: ghcr.io/univers42/grobase-db-bootstrap:latest
    ports: []
    profiles: []
    replicas: 1
  debezium:
    enabled: false
    image: ghcr.io/univers42/grobase-debezium:latest
    ports: []
    probe:
      exec:
      - sh
      - -c
      - curl -fsS http://localhost:8080/q/health >/dev/null || exit 1
      failureThreshold: 5
      initialDelaySeconds: 30
      periodSeconds: 15
      timeoutSeconds: 5
    profiles:
    - analytics
    replicas: 1
    resources:
      limits:
        cpu: 500m
        memory: 384Mi
      requests:
        cpu: 250m
        memory: 0Mi
  dynamodb-local:
    enabled: false
    image: amazon/dynamodb-local:latest
    ports:
    - 8000
    profiles:
    - engines-extra
    - hypertube
    replicas: 1
  email-service:
    enabled: false
    image: ghcr.io/univers42/grobase-email-service:latest
    ports: []
    probe:
      exec:
      - sh
      - -c
      - wget -qO- http://localhost:3030/health/live || exit 1
      failureThreshold: 5
      initialDelaySeconds: 30
      periodSeconds: 30
      timeoutSeconds: 3
    profiles:
    - legacy-node
    replicas: 1
    resources:
      limits:
        cpu: 250m
        memory: 128Mi
      requests:
        cpu: 125m
        memory: 0Mi
  function-scheduler:
    enabled: false
    image: ghcr.io/univers42/grobase-function-scheduler:latest
    ports:
    - 3027
    probe:
      exec:
      - /app/app
      - --healthcheck
      failureThreshold: 5
      initialDelaySeconds: 30
      periodSeconds: 30
      timeoutSeconds: 3
    profiles:
    - functions
    replicas: 1
    resources:
      limits:
        cpu: 250m
        memory: 96Mi
      requests:
        cpu: 125m
        memory: 0Mi
  functions-runtime:
    enabled: false
    image: ghcr.io/univers42/grobase-functions-runtime:latest
    ports:
    - 3060
    probe:
      exec:
      - deno
      - eval
      - try{const r=await fetch('http://localhost:3060/health/live');Deno.exit(r.ok?0:1)}catch{Deno.exit(1)}
      failureThreshold: 5
      initialDelaySeconds: 10
      periodSeconds: 15
      timeoutSeconds: 5
    profiles:
    - extras
    - functions
    replicas: 1
    resources:
      limits:
        cpu: 500m
        memory: 256Mi
      requests:
        cpu: 250m
        memory: 0Mi
  gdpr-service:
    enabled: false
    image: ghcr.io/univers42/grobase-gdpr-service:latest
    ports: []
    probe:
      exec:
      - sh
      - -c
      - wget -qO- http://localhost:3080/health/live || exit 1
      failureThreshold: 5
      initialDelaySeconds: 30
      periodSeconds: 30
      timeoutSeconds: 3
    profiles:
    - legacy-node
    replicas: 1
    resources:
      limits:
        cpu: 250m
        memory: 128Mi
      requests:
        cpu: 125m
        memory: 0Mi
  gotrue:
    enabled: true
    image: ghcr.io/univers42/grobase-gotrue:latest
    ports: []
    probe:
      exec:
      - sh
      - -c
      - wget -qO- http://localhost:9999/health || exit 1
      failureThreshold: 8
      initialDelaySeconds: 15
      periodSeconds: 30
      timeoutSeconds: 3
    profiles: []
    replicas: 1
    resources:
      limits:
        cpu: 250m
        memory: 128Mi
      requests:
        cpu: 125m
        memory: 0Mi
  gourmand:
    enabled: false
    image: node:20-alpine
    ports:
    - 80
    profiles:
    - gourmand
    replicas: 1
  grafana:
    enabled: false
    image: ghcr.io/univers42/grobase-grafana:latest
    ports:
    - 3000
    probe:
      exec:
      - sh
      - -c
      - curl -fsS http://localhost:3000/api/health >/dev/null || exit 1
      failureThreshold: 5
      initialDelaySeconds: 20
      periodSeconds: 15
      timeoutSeconds: 5
    profiles:
    - observability
    replicas: 1
  hypertube:
    enabled: false
    image: node:20-alpine
    ports:
    - 80
    profiles:
    - hypertube
    replicas: 1
  hypertube-api:
    enabled: false
    image: ghcr.io/univers42/grobase-hypertube-api:latest
    ports: []
    profiles:
    - hypertube
    replicas: 1
  hypertube-media:
    enabled: false
    image: ghcr.io/univers42/grobase-hypertube-media:latest
    ports: []
    profiles:
    - hypertube
    replicas: 1
    resources:
      limits:
        memory: 2048Mi
      requests:
        memory: 0Mi
  hypertube-search:
    enabled: false
    image: ghcr.io/univers42/grobase-hypertube-search:latest
    ports: []
    profiles:
    - hypertube
    replicas: 1
  hypertube-stream:
    enabled: false
    image: ghcr.io/univers42/grobase-hypertube-stream:latest
    ports: []
    profiles:
    - hypertube
    replicas: 1
    resources:
      limits:
        memory: 512Mi
      requests:
        memory: 0Mi
  iceberg-rest:
    enabled: false
    image: ghcr.io/univers42/grobase-iceberg-rest:latest
    ports:
    - 8181
    probe:
      exec:
      - sh
      - -c
      - curl -fsS http://localhost:8181/v1/config >/dev/null || exit 1
      failureThreshold: 5
      initialDelaySeconds: 20
      periodSeconds: 15
      timeoutSeconds: 5
    profiles:
    - analytics
    - extras
    replicas: 1
    resources:
      limits:
        cpu: 500m
        memory: 384Mi
      requests:
        cpu: 250m
        memory: 0Mi
  kong:
    enabled: true
    image: ghcr.io/univers42/grobase-kong:latest
    ports:
    - 8000
    probe:
      exec:
      - sh
      - -c
      - kong health
      failureThreshold: 10
      initialDelaySeconds: 20
      periodSeconds: 30
      timeoutSeconds: 3
    profiles: []
    replicas: 1
    resources:
      limits:
        cpu: 1000m
        memory: 1024Mi
      requests:
        cpu: 500m
        memory: 0Mi
  log-service:
    enabled: false
    image: ghcr.io/univers42/grobase-log-service:latest
    ports: []
    probe:
      exec:
      - sh
      - -c
      - wget -qO- http://localhost:3110/health/live || exit 1
      failureThreshold: 5
      initialDelaySeconds: 30
      periodSeconds: 30
      timeoutSeconds: 3
    profiles:
    - legacy-node
    replicas: 1
    resources:
      limits:
        cpu: 250m
        memory: 128Mi
      requests:
        cpu: 125m
        memory: 0Mi
  loki:
    enabled: false
    image: ghcr.io/univers42/grobase-loki:latest
    ports:
    - 3100
    probe:
      exec:
      - /bin/busybox
      - wget
      - -qO-
      - http://localhost:3100/ready
      failureThreshold: 5
      initialDelaySeconds: 20
      periodSeconds: 15
      timeoutSeconds: 5
    profiles:
    - observability
    replicas: 1
    resources:
      limits:
        cpu: 500m
        memory: 512Mi
      requests:
        cpu: 250m
        memory: 0Mi
  mailpit:
    enabled: true
    image: axllent/mailpit:latest
    ports:
    - 8025
    probe:
      exec:
      - /mailpit
      - readyz
      failureThreshold: 5
      initialDelaySeconds: 5
      periodSeconds: 30
      timeoutSeconds: 3
    profiles: []
    replicas: 1
    resources:
      limits:
        cpu: 100m
        memory: 64Mi
      requests:
        cpu: 50m
        memory: 0Mi
  mariadb:
    enabled: false
    image: ghcr.io/univers42/grobase-mariadb:latest
    ports:
    - 3306
    probe:
      exec:
      - healthcheck.sh
      - --connect
      - --innodb_initialized
      failureThreshold: 5
      initialDelaySeconds: 20
      periodSeconds: 10
      timeoutSeconds: 5
    profiles:
    - engines-extra
    replicas: 1
    resources:
      limits:
        cpu: 500m
        memory: 384Mi
      requests:
        cpu: 250m
        memory: 0Mi
  minio:
    enabled: false
    image: ghcr.io/univers42/grobase-minio:latest
    ports:
    - 9000
    - 9001
    probe:
      exec:
      - sh
      - -c
      - curl -fsS http://localhost:9000/minio/health/live >/dev/null || exit 1
      failureThreshold: 5
      initialDelaySeconds: 15
      periodSeconds: 15
      timeoutSeconds: 5
    profiles:
    - extras
    - storage
    replicas: 1
  minio-iceberg-init:
    enabled: false
    image: ghcr.io/univers42/grobase-mc:latest
    ports: []
    profiles:
    - analytics
    - extras
    - storage
    replicas: 1
  mongo:
    enabled: false
    image: ghcr.io/univers42/grobase-mongo:latest
    ports:
    - 27017
    probe:
      exec:
      - sh
      - -c
      - 'mongosh --host mongo --quiet --eval "try { rs.status().myState === 1 ? quit(0) : quit(1) } catch(e)
        { db.runCommand({ ping: 1 }).ok ? quit(0) : quit(1) }"'
      failureThreshold: 10
      initialDelaySeconds: 15
      periodSeconds: 30
      timeoutSeconds: 10
    profiles:
    - data-plane
    replicas: 1
    resources:
      limits:
        cpu: 500m
        memory: 512Mi
      requests:
        cpu: 250m
        memory: 0Mi
  mongo-api:
    enabled: false
    image: ghcr.io/univers42/grobase-mongo-api:latest
    ports: []
    probe:
      exec:
      - sh
      - -c
      - wget -qO- http://localhost:3010/health/live || exit 1
      failureThreshold: 5
      initialDelaySeconds: 30
      periodSeconds: 30
      timeoutSeconds: 3
    profiles:
    - data-plane
    replicas: 1
    resources:
      limits:
        cpu: 250m
        memory: 128Mi
      requests:
        cpu: 125m
        memory: 0Mi
  mongo-init:
    enabled: false
    image: ghcr.io/univers42/grobase-mongo-init:latest
    ports: []
    profiles:
    - data-plane
    replicas: 1
  mongo-keyfile:
    enabled: false
    image: ghcr.io/univers42/grobase-mongo-keyfile:latest
    ports: []
    profiles:
    - data-plane
    replicas: 1
  movieverse:
    enabled: false
    image: nginx:1.27-alpine
    ports:
    - 80
    profiles:
    - movieverse
    replicas: 1
  mssql:
    enabled: false
    image: ghcr.io/univers42/grobase-mssql:latest
    ports:
    - 1433
    probe:
      exec:
      - sh
      - -c
      - /opt/mssql-tools18/bin/sqlcmd -S localhost -U sa -P "$$MSSQL_SA_PASSWORD" -C -No -Q 'SELECT 1'
        -b -o /dev/null
      failureThreshold: 10
      initialDelaySeconds: 45
      periodSeconds: 10
      timeoutSeconds: 5
    profiles:
    - engines-extra
    replicas: 1
    resources:
      limits:
        cpu: 1000m
        memory: 2048Mi
      requests:
        cpu: 500m
        memory: 0Mi
  mysql:
    enabled: false
    image: ghcr.io/univers42/grobase-mysql:latest
    ports:
    - 3306
    probe:
      exec:
      - mysqladmin
      - ping
      - -h
      - 127.0.0.1
      - -u
      - root
      - -p${MYSQL_ROOT_PASSWORD:-mysqlroot}
      failureThreshold: 5
      initialDelaySeconds: 20
      periodSeconds: 10
      timeoutSeconds: 5
    profiles:
    - analytics
    - data-plane
    - extras
    replicas: 1
    resources:
      limits:
        cpu: 500m
        memory: 384Mi
      requests:
        cpu: 250m
        memory: 0Mi
  newsletter-service:
    enabled: false
    image: ghcr.io/univers42/grobase-newsletter-service:latest
    ports: []
    probe:
      exec:
      - sh
      - -c
      - wget -qO- http://localhost:3090/health/live || exit 1
      failureThreshold: 5
      initialDelaySeconds: 30
      periodSeconds: 30
      timeoutSeconds: 3
    profiles:
    - legacy-node
    replicas: 1
    resources:
      limits:
        cpu: 250m
        memory: 128Mi
      requests:
        cpu: 125m
        memory: 0Mi
  orchestrator:
    enabled: false
    image: ghcr.io/univers42/grobase-orchestrator:latest
    ports:
    - 3026
    probe:
      exec:
      - /app/app
      - --healthcheck
      failureThreshold: 5
      initialDelaySeconds: 30
      periodSeconds: 30
      timeoutSeconds: 3
    profiles:
    - background
    - control-plane
    - go-control-plane
    replicas: 1
    resources:
      limits:
        cpu: 250m
        memory: 32Mi
      requests:
        cpu: 125m
        memory: 0Mi
  outbox-relay:
    enabled: false
    image: ghcr.io/univers42/grobase-outbox-relay:latest
    ports: []
    probe:
      exec:
      - sh
      - -c
      - wget -qO- http://localhost:3130/health/live || exit 1
      failureThreshold: 5
      initialDelaySeconds: 10
      periodSeconds: 10
      timeoutSeconds: 3
    profiles:
    - legacy-node
    replicas: 1
    resources:
      limits:
        cpu: 500m
        memory: 256Mi
      requests:
        cpu: 250m
        memory: 0Mi
  permission-engine:
    enabled: false
    image: ghcr.io/univers42/grobase-permission-engine:latest
    ports: []
    probe:
      exec:
      - sh
      - -c
      - wget -qO- http://localhost:3050/health/live || exit 1
      failureThreshold: 5
      initialDelaySeconds: 30
      periodSeconds: 30
      timeoutSeconds: 3
    profiles:
    - adapter-plane
    - control-plane
    replicas: 1
    resources:
      limits:
        cpu: 250m
        memory: 128Mi
      requests:
        cpu: 125m
        memory: 0Mi
  pg-backup:
    enabled: false
    image: ghcr.io/univers42/grobase-pg-backup:latest
    ports: []
    probe:
      exec:
      - /opt/pg-backup/entrypoint.sh
      - liveness
      failureThreshold: 3
      initialDelaySeconds: 10
      periodSeconds: 30
      timeoutSeconds: 5
    profiles:
    - backups
    - ops
    replicas: 1
    resources:
      limits:
        cpu: 250m
        memory: 128Mi
      requests:
        cpu: 125m
        memory: 0Mi
  pg-meta:
    enabled: false
    image: ghcr.io/univers42/grobase-pg-meta:latest
    ports: []
    probe:
      exec:
      - sh
      - -c
      - node -e "fetch('http://localhost:8080/health').then(r=>{if(r.status!==200)throw 1}).catch(()=>process.exit(1))"
      failureThreshold: 5
      initialDelaySeconds: 15
      periodSeconds: 15
      timeoutSeconds: 5
    profiles:
    - extras
    - studio
    replicas: 1
  pg-migrate:
    enabled: true
    image: ghcr.io/univers42/grobase-db-bootstrap:latest
    ports: []
    profiles: []
    replicas: 1
  playground:
    enabled: false
    image: nginx:1.27-alpine
    ports:
    - 80
    profiles:
    - playground
    replicas: 1
  postgres:
    enabled: true
    image: ghcr.io/univers42/grobase-postgres:latest
    ports:
    - 5432
    probe:
      exec:
      - sh
      - -c
      - pg_isready -U ${POSTGRES_USER:-postgres} -d ${POSTGRES_DB:-postgres}
      failureThreshold: 5
      initialDelaySeconds: 90
      periodSeconds: 30
      timeoutSeconds: 3
    profiles: []
    replicas: 1
    resources:
      limits:
        cpu: 1000m
        memory: 512Mi
      requests:
        cpu: 500m
        memory: 0Mi
  postgrest:
    enabled: true
    image: ghcr.io/univers42/grobase-postgrest:latest
    ports: []
    probe:
      exec:
      - /bin/busybox
      - wget
      - -q
      - -O
      - /dev/null
      - -T
      - '4'
      - http://127.0.0.1:3000/
      failureThreshold: 3
      initialDelaySeconds: 15
      periodSeconds: 30
      timeoutSeconds: 5
    profiles: []
    replicas: 1
    resources:
      limits:
        cpu: 250m
        memory: 128Mi
      requests:
        cpu: 125m
        memory: 0Mi
  prometheus:
    enabled: false
    image: ghcr.io/univers42/grobase-prometheus:latest
    ports:
    - 9090
    probe:
      exec:
      - /bin/busybox
      - wget
      - -qO-
      - http://localhost:9090/-/healthy
      failureThreshold: 5
      initialDelaySeconds: 15
      periodSeconds: 15
      timeoutSeconds: 5
    profiles:
    - observability
    replicas: 1
  promtail:
    enabled: false
    image: ghcr.io/univers42/grobase-promtail:latest
    ports: []
    profiles:
    - observability
    replicas: 1
  query-router:
    enabled: false
    image: ghcr.io/univers42/grobase-query-router:latest
    ports: []
    probe:
      exec:
      - sh
      - -c
      - wget -qO- http://localhost:4001/health/live || exit 1
      failureThreshold: 5
      initialDelaySeconds: 30
      periodSeconds: 30
      timeoutSeconds: 3
    profiles:
    - adapter-plane
    replicas: 1
    resources:
      limits:
        cpu: 1000m
        memory: 512Mi
      requests:
        cpu: 500m
        memory: 0Mi
  realtime:
    enabled: false
    image: ghcr.io/univers42/grobase-realtime:latest
    ports: []
    probe:
      exec:
      - /app/realtime-server
      - --healthcheck
      failureThreshold: 5
      initialDelaySeconds: 30
      periodSeconds: 30
      timeoutSeconds: 3
    profiles:
    - data-plane
    - realtime
    replicas: 1
    resources:
      limits:
        cpu: 250m
        memory: 128Mi
      requests:
        cpu: 125m
        memory: 0Mi
  red-tetris:
    enabled: false
    image: node:20-alpine
    ports:
    - 80
    profiles:
    - red-tetris
    replicas: 1
  redis:
    enabled: true
    image: ghcr.io/univers42/grobase-redis:latest
    ports:
    - 6379
    probe:
      exec:
      - redis-cli
      - ping
      failureThreshold: 5
      initialDelaySeconds: 5
      periodSeconds: 30
      timeoutSeconds: 3
    profiles: []
    replicas: 1
    resources:
      limits:
        cpu: 250m
        memory: 512Mi
      requests:
        cpu: 125m
        memory: 0Mi
  schema-service:
    enabled: false
    image: ghcr.io/univers42/grobase-schema-service:latest
    ports: []
    probe:
      exec:
      - sh
      - -c
      - wget -qO- http://localhost:3060/health/live || exit 1
      failureThreshold: 5
      initialDelaySeconds: 30
      periodSeconds: 30
      timeoutSeconds: 3
    profiles:
    - control-plane
    replicas: 1
    resources:
      limits:
        cpu: 250m
        memory: 128Mi
      requests:
        cpu: 125m
        memory: 0Mi
  session-service:
    enabled: false
    image: ghcr.io/univers42/grobase-session-service:latest
    ports: []
    probe:
      exec:
      - sh
      - -c
      - wget -qO- http://localhost:3120/health/live || exit 1
      failureThreshold: 5
      initialDelaySeconds: 30
      periodSeconds: 30
      timeoutSeconds: 3
    profiles:
    - legacy-node
    replicas: 1
    resources:
      limits:
        cpu: 250m
        memory: 128Mi
      requests:
        cpu: 125m
        memory: 0Mi
  storage-router:
    enabled: false
    image: ghcr.io/univers42/grobase-storage-router:latest
    ports: []
    probe:
      exec:
      - sh
      - -c
      - wget -qO- http://localhost:3040/health/live || exit 1
      failureThreshold: 5
      initialDelaySeconds: 30
      periodSeconds: 30
      timeoutSeconds: 3
    profiles:
    - extras
    - storage
    replicas: 1
    resources:
      limits:
        cpu: 250m
        memory: 128Mi
      requests:
        cpu: 125m
        memory: 0Mi
  studio:
    enabled: false
    image: ghcr.io/univers42/grobase-studio:latest
    ports:
    - 3000
    probe:
      exec:
      - sh
      - -c
      - 'node -e "fetch(''http://127.0.0.1:3000/api/profile'').then(r => process.exit(r.status < 500 ?
        0 : 1)).catch(() => process.exit(1))"'
      failureThreshold: 5
      initialDelaySeconds: 30
      periodSeconds: 10
      timeoutSeconds: 5
    profiles:
    - extras
    - studio
    replicas: 1
    resources:
      limits:
        cpu: 500m
        memory: 384Mi
      requests:
        cpu: 250m
        memory: 0Mi
  supavisor:
    enabled: false
    image: ghcr.io/univers42/grobase-supavisor:latest
    ports: []
    probe:
      exec:
      - sh
      - -c
      - wget -q -O /dev/null -T 4 http://127.0.0.1:4000/api/health || exit 1
      failureThreshold: 5
      initialDelaySeconds: 30
      periodSeconds: 15
      timeoutSeconds: 5
    profiles:
    - extras
    - ops
    replicas: 1
  surfind:
    enabled: false
    image: node:20-alpine
    ports:
    - 80
    profiles:
    - surfind
    replicas: 1
  tenant-control:
    enabled: false
    image: ghcr.io/univers42/grobase-tenant-control:latest
    ports:
    - 3022
    probe:
      exec:
      - /app/app
      - --healthcheck
      failureThreshold: 5
      initialDelaySeconds: 30
      periodSeconds: 30
      timeoutSeconds: 3
    profiles:
    - control-plane
    - go-control-plane
    replicas: 1
    resources:
      limits:
        cpu: 500m
        memory: 160Mi
      requests:
        cpu: 250m
        memory: 0Mi
  tmdb-proxy:
    enabled: false
    image: ghcr.io/univers42/grobase-tmdb-proxy:latest
    ports: []
    profiles:
    - movieverse
    replicas: 1
  trino:
    enabled: false
    image: ghcr.io/univers42/grobase-trino:latest
    ports: []
    probe:
      exec:
      - sh
      - -c
      - curl -fsS http://localhost:8080/v1/info >/dev/null || exit 1
      failureThreshold: 5
      initialDelaySeconds: 60
      periodSeconds: 15
      timeoutSeconds: 5
    profiles:
    - analytics
    - extras
    replicas: 1
    resources:
      limits:
        cpu: 2000m
        memory: 2048Mi
      requests:
        cpu: 1000m
        memory: 0Mi
  vault:
    enabled: false
    image: ghcr.io/univers42/grobase-vault:latest
    ports:
    - 8200
    probe:
      exec:
      - sh
      - -c
      - vault status -address=http://127.0.0.1:8200 2>/dev/null | grep -q 'Sealed.*false' || exit 1
      failureThreshold: 10
      initialDelaySeconds: 15
      periodSeconds: 30
      timeoutSeconds: 3
    profiles:
    - secrets
    replicas: 1
    resources:
      limits:
        cpu: 500m
        memory: 256Mi
      requests:
        cpu: 250m
        memory: 0Mi
  vault-init:
    enabled: false
    image: ghcr.io/univers42/grobase-vault-init:latest
    ports: []
    profiles:
    - secrets
    replicas: 1
  vault42:
    enabled: false
    image: ${VAULT42_IMAGE:-docker.io/dlesieur/vault42-server:latest}
    ports:
    - 8443
    profiles:
    - vault42
    replicas: 1
  waf:
    enabled: true
    image: ghcr.io/univers42/grobase-waf:latest
    ports:
    - 80
    - 443
    probe:
      exec:
      - sh
      - -c
      - grep -qi ':0050 .* 0A' /proc/net/tcp || exit 1
      failureThreshold: 3
      initialDelaySeconds: 5
      periodSeconds: 10
      timeoutSeconds: 3
    profiles: []
    replicas: 1
    resources:
      limits:
        cpu: 500m
        memory: 256Mi
      requests:
        cpu: 250m
        memory: 0Mi
  webhook-dispatcher:
    enabled: false
    image: ghcr.io/univers42/grobase-webhook-dispatcher:latest
    ports:
    - 3025
    probe:
      exec:
      - /app/app
      - --healthcheck
      failureThreshold: 5
      initialDelaySeconds: 30
      periodSeconds: 30
      timeoutSeconds: 3
    profiles:
    - background
    - control-plane
    - go-control-plane
    replicas: 1
    resources:
      limits:
        cpu: 250m
        memory: 96Mi
      requests:
        cpu: 125m
        memory: 0Mi
