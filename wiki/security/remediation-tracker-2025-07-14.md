# Security Remediation Tracker

Full audit: [`vulnerability-report-2025-07-14.md`](vulnerability-report-2025-07-14.md) (61 findings).

**Re-verified 2026-09-24 against the code, not the report.** The previous version of this file
marked ~20 findings "✅ Fixed", but those edits had been reverted (the diff was parked as
`.bob/security-diff-reverted.patch`, since deleted) and several of them were regressions: they
broke the Prometheus→Kong scrape, the Vault init container, the Fly Vault boot, engine backups
(m188) and webhook TLS. Every row below was checked in the code; every ✅ names the commit and
the test or gate that goes red without the fix. Work lands on `develop` (git-flow, one branch
per problem).

**Legend** — ✅ fixed and proven · 🟡 needs a human / product decision · 📌 open, tracked ·
⚪ false positive or already mitigated (evidence given) · ➖ accepted by design.

## Needs a human (do these first)

| # | What | Why it cannot be done in code |
|---|---|---|
| **N-2** | Ask GitHub Support (support.github.com → remove sensitive data) to drop cached views and the `refs/pull/*` of PRs #1–#36, which still point at pre-purge commits (first changed commit `2fd2cc1f0bf5892794428a0814b517e0d400663f`). | Branches and tags are purged (see Fixed); GitHub-managed PR refs and cached commit pages are not writable by the repo owner. The key itself cannot be revoked: the Unsplash account is no longer accessible. |
| **C-1** | Revoke the leaked GitHub PAT at github.com/settings/tokens, then rotate anything that used it. | Issuer-side action. The value is no longer on the dev machine (checked 2026-09-25: `.env*`, home, containers, shell history, git history — only the fake fixture), and GitHub's revocation API needs the value. `fix/pat-out-of-env` (m-series, on main) stops `make env` from carrying GitHub tokens into `.env`. 2026-09-25: the classic PAT later put in `.env` as `GH_PAT` (over-scoped, and injected into every container through `env_file`) was revoked through `POST /credentials/revoke` (202; `GET /user` with it then 401) and dropped from `.env`. The audit redacted its token, so whether that was the C-1 token is unknown — if C-1 was a different token, it still needs revoking at github.com/settings/tokens. |
| **L-11** | Require signed commits on `main` (GitHub → Branches). | Repository setting. |
| **Vault base image** | `infra/docker/services/vault/Dockerfile` is `vault:1.21` (last 1.21 tag), `Dockerfile.fly` is `vault:1.16`; both carry fixable HIGH/CRITICAL CVEs (see `.trivyignore` note). Fix = Vault 2.x. | Major upgrade that ships to deployments (storage format, unseal) — needs a planned rollout. |

Deployment target: **self-hosted** (`make prod-up` = enforced preflight + prod overlay). fly.io is retired
(2026-09, cost); `deploy/fly/` is kept, not deleted, and its `boot.sh` changes ship only if it returns.

## Needs a product / architecture decision

| # | Finding | Evidence | Options |
|---|---|---|---|
| **H-16** | `deploy/fly/boot.sh` sets `GOTRUE_MAILER_AUTOCONFIRM=true`: sign-up without owning the address. | Fly is retired. The self-hosted path (`make prod-up`) layers the prod overlay, which sets `false` (m195) and needs a real `SMTP_HOST`. `boot.sh:61` is `${GOTRUE_MAILER_AUTOCONFIRM:-true}` if Fly ever returns. | Decide before a Fly return; confirm the website/vault42 frontends handle the confirmation step before a self-hosted go-live. |
| **H-4** | Verify-cache TTLs (Go 60 s, TS 30 s) bound revocation latency. | Go revocation already evicts both local and data-plane caches. The artifact now exists — `make bench-verify-hash` → `artifacts/bench/verify-hash/2026-09-25T17-43-21Z.json` (i5-13600KF, `-cpu 4`, `-benchtime 2s`): a **fast (SHA-256) verify costs 284 ns, 384 B, 6 allocs** (~3.5 M/s), while a **legacy argon2id verify costs 9.1 ms and allocates 32 MiB** — ~108/s at `ARGON2_MAX_CONCURRENT=1`, ~203/s at the default 2, ~234/s at 4. The 60 s TTL was sized for the argon2 figure; new keys are minted on the fast scheme and legacy ones are rewritten to it on first verify (`keys_hash.go:selectHash`, `keys_verify.go:77`). | **Lowering is now affordable for a fleet on the fast scheme** — a 5 s TTL costs ~1.4 µs of hashing per key per 5 s. Two caveats before flipping: the artifact measures the HASH only, so a TS cache miss still costs an HTTP round trip to tenant-control plus its Postgres lookup (unmeasured), and a deployment with `KEY_HASH_LEGACY_ARGON2=1` or un-migrated keys keeps the ~203/s ceiling. Pick the TTL per deployment. |
| **M-9 (rest)** | `REALTIME_NAMESPACE_FALLBACK=deny` in production (the third value M-9 asked the overlay to enforce). | GoTrue session JWTs carry no `namespaces` claim, so `deny` closes every realtime topic to browser clients. The prod overlay leaves it as-is on purpose (m195 `static_held` asserts it unchanged); the preflight warns when it is not `deny` (m194). `REALTIME_PROTECTED_NAMESPACES` already guards `collab:` (m175). | Build a namespace-minting path (a GoTrue hook or a realtime token exchange), then set `deny` in the prod overlay; or accept permissive-plus-protected-prefixes. |
| **Helm / go-live path** | Whether to keep `deploy/helm/grobase` + `deploy/go-live/go-live.sh` as a deploy target. | Found while wiring the preflight (plan item: preflight into go-live). The chart deploys postgres, redis, data plane, tenant-control, adapter-registry, realtime and Kong only (no GoTrue, no Nest services); its render carries no `POSTGRES_PASSWORD`, `JWT_SECRET` or `DATABASE_URL` — credentials come only from Vault CSI (`vault.enabled: false` by default) or `env.secret.data`, and go-live passes neither, so a go-live release cannot boot as-is. Default registry `ghcr.io/les-baas` is stale (images are `ghcr.io/univers42/grobase-*`). The compose preflight does not apply: the chart takes no `.env`. | Retire the Helm path (self-hosted compose is the production target), or bring it to parity: the missing planes, a credential contract go-live validates, the right registry — which needs a cluster to prove. |
| **H-5** | An `admin`-scoped API key short-circuits ABAC (`query.service.ts:774`). | By design; m139 asserts `API_KEY_ABAC_ENABLED` defaults off. The bypass is no longer silent — it is logged (H-5a below), so a log reader can tell "the PDP allowed this" from "the PDP never ran" and count how many tenants are relying on it. | Turn the flag on per deployment after testing policies; the log tells you which tenants and subjects it would start affecting. |

## Fixed and proven

| ID | Finding (as re-verified) | Fix | Proof (red before → green after) |
|---|---|---|---|
| N-2 | A live Unsplash access key was committed on 2026-06-18 (`vendor/vite-gourmand/Back/scripts/update-menu-images.ts:18`, deleted from the tree the next day, still in history); it cannot be revoked (account no longer accessible) | History purge 2026-09-25: `git filter-repo --sensitive-data-removal --replace-text` on every branch and tag (402 of 1065 commits rewritten, first changed = a merge that already held the key), force-pushed with the `main` ruleset briefly off; the dated `.trufflehog-accepted` exception is removed | Every object reachable from the rewritten refs scanned for the key: 0 (1 in the original); the Security workflow's trufflehog job passes with no exception |
| C-2 | `ipguard`/`audit` self-auth compared the raw `X-Baas-Tenant-Id` and ignored `TENANT_HEADER_IDENTITY_HMAC` | `cd55d9a2` | `self_auth_test.go` ×2 (forged header authorized → rejected) |
| C-7 | No proof PostgREST logs in as a role RLS applies to; track-binocle fallback named the superuser | `e88d10c7` | m200 (3): authenticator NOSUPER/NOBYPASSRLS, sessions only authenticator; mutant `BYPASSRLS` → red |
| H-1 | Prod overlay `ports: []` stripped nothing (compose merges lists); minio got `0.0.0.0:9000` | `83c5d83b` | m195 `static_ports` |
| H-11 | supavisor build used unverified hex/rebar3 downloads | `7bc2c04f` | pinned RUN block: real pins build, tampered pin refused |
| H-13 | A write key could revoke the tenant's admin keys | `78c9db31` | m83 step 4e2 (200 → 403), in CI cloud-gates |
| H-15 | Five localhost dev origins had credentialed CORS in production | `3211e86c` | m195 CORS preflight (prod allowed localhost:5180 → refused); phase 13 13/13 |
| H-19 | `IDENTITY_HEADER_MODE=compat` in production: services trusted raw `X-User-*` headers, so anything reaching a service past Kong could claim any user | `e3def4b1` — the prod overlay sets strict on the 11 Nest services that read it (`PROD_IDENTITY_HEADER_MODE=compat` reverts); built on H-19a (signer), H-19c (owner parity) | m202 in CI integration (strict: real session 200, compat-written object still its owner's; raw header / cross-app / wrong-secret 401) · m195 `static_identity` (strict on exactly the 11; mutants that drop a reader, widen strict or set the HMAC flag go red) |
| H-19d | The managed-cloud overlay (`make cloud-up`) kept compat identity while prod went strict, and `make cloud-up` never ran the preflight | `fix/cloud-strict-identity` — `docker-compose.cloud.yml` sets strict on the same 11 readers (same `PROD_IDENTITY_HEADER_MODE=compat` revert); `cloud-up` runs `preflight-production.sh` warn-only (it is also the local mock-Stripe edition on a dev `.env`), `PREFLIGHT_ENFORCE=1` refuses | m195 `static_cloud_identity`: red on the old overlay, green after; a cloud overlay missing mongo-api goes red; `make _cloud-preflight` warns rc 0, with `PREFLIGHT_ENFORCE=1` exits non-zero |
| H-22 | Vault image built green without executable scripts | `bbf1be51` | build without scripts: exit 0 → exit 1 |
| M-1 | Data-plane 502 bodies echoed backend hosts/URLs | `f75ada7b` | `routes::tests::backend_error_body_hides_internal_detail` |
| M-3 | User JWT without `exp` never expired; future `iat` accepted (TS + Go) | `910727f8` | `api-key.middleware.spec.ts`, `jwt_time_test.go` |
| H-6 | A rotated-out service token (`INTERNAL_SERVICE_TOKEN_PREV`) was accepted silently | `fix/h6-service-token-rotation-signal` — `serviceauth.Verify` returns which token matched (both still compared, current wins); a `RotationNotice` logs ONE warning per process and counts `baas_service_token_previous_accepted_total`, never the token. Wired into every tenant-control service-token route (incl. `POST /v1/keys/verify`) and adapter-registry; the 16 other guards keep the plain bool (one-line switch each). | `match_test.go`, `rotation_test.go` (logs once, no token in output, 0 allocs on the current-token path); fuzz + rotation tests unchanged |
| H-14 | The identity-envelope replay cache was per process (a replay to another replica passed) | `fix/h14-identity-nonce-store` — `NonceStore` port; memory by default (parity), `IDENTITY_NONCE_STORE=redis` = `SET NX PX` shared store, fail-closed, 2 s timeouts; unknown kinds refused. Also: a malformed `INTERNAL_IDENTITY_MAX_SKEW_MS` no longer disables the freshness check. | `nonce-store.spec.ts`, `request-identity.spec.ts` (cross-replica replay rejected; guard-level replay 401 for all three guards; malformed skew) |
| M-8 | `SECURITY_MODE=max` never denied (compose passed `permissive`) | `82a9ea82` | m195 `static_realtime_mode`; `fallback_tests` |
| M-11 | Key issue/revoke left no trace | `12ccee2b` | m83 step 4e3 (log lines for the key id) |
| M-12 | Bucket policy: presign skipped it; a bucket named `constructor` bypassed a `*` deny | `34af96f1` | `bucket-policy.spec.ts` |
| M-16 | Automation rule names could forge log lines (CR/LF) | `61514472` | `automations.service.spec.ts` |
| M-17/L-9 | Presigned GETs of SVG/HTML rendered on a browsable origin | `5807534a` (+ `a47f906a` proxied path) | `presign-active-content.spec.ts` |
| L-6 | One small image upload + transform OOM-killed storage-router (measured: 151 MP under the 128 MiB limit) | `f3dc5cfc` | `image-transform.spec.ts`; measurements in the commit |
| H-20 | ShellCheck SC2086 disabled globally | `fix/sec-h20-quoting` | CI shellcheck job, step *Quoting (SC2086) on untrusted-input scripts*: `--norc -i SC2086` over the 58 container-entrypoint / deploy / ops / db / env / secrets / vault scripts — red before (8 hits), green after. None of the 8 was exploitable (the `export PGPASSWORD=${…}` split only on dash < 0.5.11); fly's `bring_up` splits `$DC`/`$SERVICES` on purpose (function-level directive). |
| M-4 | Realtime did not check `iss`: any same-secret token (a foreign-issuer bridge token, one with no `iss`) opened a session — reproduced live, `osionos-bridge` got AUTH_OK | `fix/sec-m4-issuer-expression` (one issuer expression for GoTrue + tenant-control) + `fix/sec-m4-realtime-issuer` | Realtime takes a comma-separated `REALTIME_JWT_ISSUER` allow-list and then requires `iss` (`REALTIME_JWT_ALLOW_NO_ISSUER=1` opts out). Compose lists the GoTrue issuer, `supabase` (anon/service-role keys — the SDK's anonymous realtime token — and the seed tokens, so no re-issue) and `grobase-realtime` (xapp channel tokens, `seed-live-demo`, m22, m23; tenant-control's user-session verifier refuses it, so a tenant-slug `sub` never passes as a user). `issuer_tests.rs` (red without the required-`iss` rule), `TestRealtimeClaimsIssuer`, **m201** in CI integration. |
| L-12 | Webhook DNS rebinding | `2463fd66` (IP-pinned connect-time lookup; the reverted IP-rewrite broke TLS) | `automations.pin.spec.ts` |
| C-3 | Kong admin API on the flat network | `e92133b1` (prod: admin off, status listener keeps /metrics) | m195 throwaway Kong |
| C-5 | Vault on fly ran as root | `3368756b` (`VAULT_DROP_PRIVILEGES_ENABLED`) | m196 |
| M-9 | Production relied on someone overriding `config.env`'s dev values: `GOTRUE_MAILER_AUTOCONFIRM=true`, `IDENTITY_HEADER_MODE=compat` | The prod overlay pins autoconfirm `false` (`fix/gotrue-compose-params`) and strict identity on the 11 readers (`e3def4b1`, H-19); the cloud overlay matches (H-19d); `make prod-up` runs the enforced preflight, which refuses autoconfirm `true` | m195 (`static_overlay`, `static_identity`, `static_cloud_identity`), m194. The third value, namespace `deny`, is a decision — see above. |
| L-10 | `GOTRUE_SITE_URL=https://localhost:5173` would put localhost in production confirmation/reset links | `preflight-production.sh` flags a localhost `GOTRUE_SITE_URL` (and `API_EXTERNAL_URL`); `make prod-up` refuses on it | m194 case `GOTRUE_SITE_URL=http://127.0.0.1:5173` → named offender |
| N-15 | `deploy/go-live/go-live.sh` could never run: it looked for the cloud manifest at the pre-flatten `config/cloud/`, and `flags.env.example` — the template `make cloud-up` tells operators to copy — lacked 12 names go-live sets, so its drift check refused any manifest made the documented way. One of the 12 was `DATA_PLANE_QUOTA_ENFORCEMENT`, the data-plane half of quota enforcement: a copied example with `QUOTA_ENFORCEMENT=1` enforced nothing (the silent AND no-op). | `fix/go-live-cloud-flags-path` — path repointed to `infra/config/cloud/`; the example carries the 12 names at their code defaults (still all OFF) | dry run with every input set (fake values, `helm template` via pinned `alpine/helm:3.16.2`, a kubectl shim that refuses): before, FATAL manifest not found; after the path fix, FATAL drift on 12 names; after, 29 manifests rendered, no drift, kubectl never called |
| L-4 | Verify gates had no timeout: a gate stuck on a port that never opens hung the battery until the CI job limit (180 min for the enterprise set) killed the run with no per-gate summary | `fix/gate-battery-timeout` — `run-gate-battery.sh` bounds each gate by `GATE_TIMEOUT` (default 1800 s, `0` = unbounded): SIGTERM so the gate's EXIT trap still removes its containers, SIGKILL 60 s later, reported as `FAIL … timed out` | a gate that sleeps 600 s: battery still running at 15 s (killed from outside, no summary) → with `GATE_TIMEOUT=5` it FAILs at 5 s, its EXIT trap ran, the next gate (m203) still ran and passed; m157 + m188 pass under the bound |

## Found during re-verification (not in the audit)

| ID | Finding | Fix | Proof |
|---|---|---|---|
| N-1 | `public.schema_registry` (cross-tenant table catalog) readable, insertable and deletable with the anon key | `90c122e0`, migration 088 | m200 (invariant + live 401) |
| **N-3** | A table created through the DDL API on a mount pointing at the platform database — or any table nothing granted — was readable/writable with the anon key via PostgREST: `db-bootstrap` + `001` granted anon/authenticated on every existing AND future `public` table, and bootstrap re-granted every boot (fly never re-runs migrations, so a reboot undid 088). On a fresh install every control-plane table had anon read+write with RLS as the only barrier. Option (b) taken: no blanket/default grant; bootstrap revokes the default every boot; migration 089. | `fix/sec-n3-default-privileges` | m200 step 4 (fresh ungranted table: 200 → 401); old-vs-new fresh-install ACL diff over 67 tables (authenticated keeps every explicit grant); CI integration phases on a fresh stack |
| N-4 | Vault unseal key + root token written `0644`; a lost key file wiped all Vault storage | `03b69632` | m199 |
| N-5 | `h2` 0.3 (RUSTSEC-2026-0258) in the DynamoDB build fly runs | `5047bb02` | `make audit-deps`: FAIL → OK; live dynamodb-local round trip |
| N-6 | 17 fixable HIGH npm advisories (multer, sharp, nodemailer, axios, js-yaml, …) | `d40b2909` | trivy fs 17 → 0; jest 445/445; m55 + phase 9 live |
| N-7 | SAST scanner reported "clean" on a missing report; real semgrep verdict was 1 ERROR | `c628adb8` | semgrep 0 ERROR; missing report → FAIL |
| N-8 | Gates that could not fail: m157 (swallowed render errors), m198 (never booted), m5/m60 (monorepo paths, formatter-mangled key) | `8e74a577`, `132c995e`, `9dba37ec` | each red on a broken input, green on the real one |
| N-9 | No security scanner ran in CI | `9dba37ec` | `.github/workflows/mini-baas-security.yml` + blocking `security-gate`; m5, m60 green |
| N-10 | `postgres/Dockerfile` wrote placeholder "sha256:…-pinned-by-mini-baas" strings into a manifest claiming pinned FDW builds; m6 "proved" the pins by grepping the ARG names; the FDWs are not compiled in at all | `fix/fdw-manifest-honest` | manifest states not-built/built-in with real versions (partial image build printed); m6 fails on any placeholder pin |
| N-11 | Realtime sometimes closed a refused connection WITHOUT sending AUTH_FAILED (measured live: 6 of 20), so a client could not tell an auth refusal from a network drop: the writer's `select!` was unbiased and the goodbye raced the queued error frame | `fix/sec-m4-realtime-issuer` (`writer.rs`: `biased`, control frames before the goodbye) | `test_auth_failed_frame_precedes_close` (multi-thread runtime, 200 refusals): red 3/3 before, green 5/5 after |
| M-13 / L-5 | GoTrue password length and refresh-reuse window hard-coded in base compose | `fix/gotrue-compose-params` | renders: base 8/10, .env override 14, prod 12 |
| **N-13** | Kong's pre-function clears the forgeable `X-Baas-Tenant-Id` / `X-Baas-User-Id` / `X-Tenant-Id` only on `/functions/` and `/query/`. `/storage/v1` also reaches a Nest service that acts on the tenant: storage-router runs in compat mode (`STORAGE_IDENTITY_HEADER_MODE:-compat`) and passes `user.tenantId` — a raw header there — as the `storage.bytes` metering dimension, so any authenticated caller could bill another tenant's usage and, under `QUOTA_ENFORCEMENT`, exhaust their storage quota. Owner scoping is by the JWT-derived `X-User-Id` and was never affected (m55 step 7); no other Nest service reads `tenantId` at all. | `fix/sec-storage-meter-tenant`: Kong clears the three headers on `/storage/v1` too (`/admin/v1` keeps them on purpose — service-role + ip-restricted, and the in-browser mount catalog scopes by them), and the meter takes its tenant only from a cryptographically verified identity, `STORAGE_METER_TRUST_RAW_TENANT=1` to opt back in | `storage-meter-tenant.spec.ts` (12): RED 5 failures before the controller fix, GREEN after; `kong config parse` successful on the rendered config; m77 opts in explicitly (it reaches the container directly, which is the trusted-proxy shape) |
| **H-19b** | Nothing proved that strict mode works for real traffic — the unit suite exercises the library, not a running service with a live GoTrue token. | `m202-strict-identity-serviceable.sh` starts a second storage-router process in strict mode _inside_ the running container (the deployed image and env, only `IDENTITY_HEADER_MODE`/`PORT` overridden), so it needs no recreate and no posture change on the stack. Against it: a real GoTrue session must be accepted, while a raw `X-User-Id`, a cross-app realtime token whose `sub` is a tenant slug, and a token signed with another secret are each refused; the compat instance must accept that same raw header, which is what makes the strict 401 non-vacuous. Tokens are signed and probes sent inside the container, so `JWT_SECRET` never reaches the host (the first version copied it into a host shell variable). Statically, every user-token verifier must agree on one `GOTRUE_JWT_ISSUER`. | **Negative half proven live** on the deployed build (2026-09-25): the same raw `X-User-Id` request got **200 from compat and 401 from strict**. **Positive half NOT yet green**: that build (GHCR `:latest`, created 2026-09-24 22:42) predates the bearer-JWT rung (`d759c100`, 2026-09-25 19:02) and its container has no `GOTRUE_JWT_ISSUER`, so a real GoTrue session got 401 from both — expected of that image, and exactly what the gate is for. The gate now fails on that first, naming the fix (`docker compose build storage-router && make up`); recreating needs an assembled `.env`, which this machine does not currently have. |
| **H-19a** | The reason production could not go strict: only the api-key route could produce an identity strict mode accepts (`ApiKeyMiddleware` self-signs an envelope), so a plain GoTrue JWT caller — every browser request Kong forwards — had no verified identity at all and strict would have 401'd it. The JWT verification primitive existed but was a private method on that one middleware, reachable only by casting (`api-key.middleware.spec.ts` did exactly that). | `fix/h19-jwt-identity-path`: the primitive is extracted to `src/libs/common/src/identity/user-jwt.ts` (`verifyUserJwt` + `bearerToken`, stdlib HMAC-SHA256, now with optional issuer pinning) and `resolveRequestIdentity` gains a bearer-JWT rung ordered AFTER legacy headers, so compat resolves exactly as before. tenant/project come from `app_metadata` (GoTrue does not let a user write it) falling back to `sub`; `roleNames`/`scopes` derive from the token alone, never from the client's `x-baas-*` headers; `sub` is required, which is what rejects the subject-less anon/service_role project keys. Active in strict mode unconditionally, in compat only under `IDENTITY_JWT_BEARER_ENABLED=1` (default off = byte-parity). | `request-identity.spec.ts` + the moved `user-jwt.spec.ts`: RED 5 failures on the absent rung, GREEN 509/509 across all 27 suites. Four mutations killed: dropping the compat opt-in gate, dropping the `GOTRUE_JWT_ISSUER` pin, accepting a subject-less token, and ordering the JWT rung before legacy. m11 extended with the rung (accept + ignore spoofed `x-user-id` + expired reject + compat-inert) and re-mutation-tested red on the parity case. |
| **H-19c** | Found while checking the strict flip, before it shipped: the bearer-JWT rung (H-19a) resolved `userId` to `user:<sub>` and `appId` to `jwt`, while the legacy path Kong feeds today resolves the same token to the raw `<sub>` and `legacy`. `identityToUserContext` hands `userId` to the services verbatim (`request-identity.ts:78`), and they key data on it — storage object paths are `${userId}/…`, mongo-api stamps and filters `owner_id: userId`, and bucket-policy principals become `user:${userId}`. So flipping strict would have hidden every file and document users already own. Inert until the flip (compat default, `IDENTITY_JWT_BEARER_ENABLED=0`); m202 missed it because a bucket listing does not touch the owner key. | `fix/h19-jwt-identity-parity`: the rung yields the raw `sub` and `appId: 'legacy'`, exactly what readLegacyIdentity derives from the X-User-* headers Kong sets for that token. m202 gains an owner-continuity step: an object written through compat with Kong's headers must be readable by its owner through strict with only the bearer (same bytes), and not by a second user. | `request-identity.spec.ts` 'gives a Kong-forwarded user the SAME context in strict as in compat': RED (`userId: "user:1e6a…"` vs `"1e6a…"`, `appId: "jwt"` vs `"legacy"`), GREEN 517/517; m11 updated to assert the raw `sub` and still passes. |
| **H-5a** | The `admin`-scope ABAC short-circuit left no trace: `decideByApiKeyScope` returned `{allow:true, reason:'api-key admin scope'}` without logging, so nothing in the logs distinguished "the PDP evaluated this and allowed it" from "the PDP never ran", and an operator had no way to learn which tenants would be affected by turning `API_KEY_ABAC_ENABLED=1` on. The audit reported the bypass; it did not report that the bypass was invisible. | `fix/h5-log-abac-bypass`: `announceAbacBypass` logs tenant + subject + the flag to set, rate-limited per `tenant\|subject` by `ABAC_BYPASS_LOG_INTERVAL_MS` (default 300000) so one busy admin key cannot drown the others, with the seen-map bounded at 256 entries and swept on overflow. Authorization behaviour is unchanged — the bypass still allows, it just says so. | `query.service.spec.ts` `describe('QueryService admin-scope ABAC bypass is observable (H-5)')`: RED 3 of 4 failing on the silent path, GREEN 516/516 across all 27 suites. Two mutations killed: keying the rate limit on tenant alone (`✕ logs each distinct subject separately`) and neutering the rate-limit guard (`✕ logs a repeat subject once, not once per request`). |
| **N-12** | Privilege escalation on the signed identity path: `canonicalIdentityString` bound method/path/tenant/project/user/role/app/iat/nonce/body but NOT `roles` or `scopes`, while `readSignedIdentity` built `roleNames` from an unsigned `X-Baas-Roles` and `scopes` from an unsigned `X-Baas-Scopes` (Kong strips neither, `signIdentityEnvelope` emitted neither). `ApiKeyMiddleware` signs an envelope for anyone holding a valid api key and stamps it onto the client's own request, so `X-Baas-Roles: service_role` survived into `identity.roleNames` → `RolesGuard` + the ABAC PDP; a forged `admin` scope reached `decideByApiKeyScope` → `{allow:true}`. Narrowed by: only query-router mounts that middleware, `@Roles` endpoints take the legacy path (`roleNames=[role]`), and `keymgmt.go:50` defaults empty scopes to `read,write`. | `fix/sec-signed-identity-binds-roles-scopes`: `roles`/`scopes` (deduped + sorted) signed into the canonical string; the signer always emits both headers so a client copy is overwritten, not merged; Kong clears both on `/functions/` and `/query/` | `request-identity.spec.ts`: injected `x-baas-roles`/`x-baas-scopes` tamper cases + an api-key-middleware escalation test — RED 3 failures (`roleNames: ["service_role","authenticated"]`), GREEN 52/52; m11 extended with both negatives and now runs (its runtime step had been dead since `2618ff4a` made `resolveRequestIdentity` async without an `await`) |
| N-14 | Migrations 038, 039, 087, 088 and 089 never recorded themselves in `schema_migrations`; they re-ran every boot, but `make migrate-status` never listed them, so nobody could confirm a deployment had the two security migrations (088 schema_registry, 089 default privileges) | `fix/migrations-record-themselves` | m203 (static: a new unrecorded or duplicate-numbered migration goes red; live: every version on disk recorded), in CI integration |
| **N-16** | Security events were invisible to monitoring. The Nest request counter was an interceptor, and neither a middleware that answers 401 itself (`ApiKeyMiddleware`) nor a guard that throws 401/403 reaches one — measured live: two `AuthGuard` 401s on query-router, `mini_baas_http_requests_total` unchanged, no `http_request` log line. There were no alert rules on 401/403/429 at all. Two critical `TargetDown` alerts fired permanently on the max tier (vault and schema-service, which it never runs), and the gate for the alert rules, m52, had failed at its first line since the flatten (`config/prometheus`) without anyone noticing, because it was not in CI. | `feat/security-event-observability`: a request-metrics middleware counts on response finish in a now-global `ObservabilityModule`; the route label stays a registered pattern. The `http_request` line carries `event_type` (`auth_failure`/`access_denied`/`rate_limited`) and promtail makes it a Loki label. A `platform-security` rule group covers 401/403/429 spikes at Kong, 401/403 inside services, and the previous service token still in use. Edition-optional targets move to `dns_sd_configs`, with `TargetVanished` covering a stopped container. query-router, storage-router, analytics and ai are now scraped. | `request-metrics.spec.ts`: RED 5/6 on the interceptor, GREEN 6/6; dropping `@Global()` fails the middleware-401 case. `tests/platform.test.yml` (promtool): 5 mutants killed (threshold, `unless`, 404 in the regex, the rotation window, the 429 code) plus the never-up case. m52 is red live on the old config (2 phantom targets) and green after, and now runs in CI's lint job. m204 is green live on the max tier: 20×401 through Kong in both counters, the log, both alert expressions and Loki. |
| N-17 | Assessment §6.6 asked for a load baseline in CI. The load gate already existed (m38), but nothing ran it, and it could not have passed. `scripts/lib/lib-bench.sh` moved in the scripts reorg and still looked for `k6/` and `budgets.json` beside itself, so every k6 bench and m38 failed before sending a request. `load.sh`'s `\|\| echo 50` also turned the missing budget into a silent 50 rps. | `fix/bench-lib-paths`: the lib reads `scripts/bench/` and refuses to load without `budgets.json`. `realtime-fanout.sh` gets its path fixed too (pre-flatten `docker/services/`). The new nightly `load-baseline` job boots the query edition and runs m38 with `M38_REQUIRE_STACK=1`, so a stack that never came up fails instead of SKIPping. The step is fatal. | m38 was red (`/scripts/crud.js couldn't be found`) and is now green: `PACKAGE=essential` gives 20.02 rps, p95 5.43 ms, 0 % errors (`artifacts/bench/load-essential-crud.json`, this host with the max stack up) against the bars p95 ≤ 60 ms, err ≤ 1 %. `M38_REQUIRE_STACK=1` gives rc 1 where the SKIP gives rc 0. The nightly job has not run yet: no GitHub token, so its first result is **UNKNOWN**. |
| N-18 | G-Rotate, service-token half: the Go verifiers already accepted `INTERNAL_SERVICE_TOKEN_PREV` (m68), but compose never passed it to any container, the TS `ServiceTokenGuard` knew only one token, and no procedure existed. The Rust helpers for a previous token are unused because the data plane only sends the token. | `feat/g-rotate-service-token`: the guard accepts `ADAPTER_REGISTRY_SERVICE_TOKEN_PREV` in constant time and counts it in `baas_service_token_previous_accepted_total`. Compose maps the source key onto the 8 `INTERNAL_SERVICE_TOKEN` holders and the 3 services that set the TS name explicitly. `scripts/ops/rotate-service-token.sh begin|swap|finish` edits `.env.secrets` (dry run unless `--apply`, lengths only) and reassembles `.env`. The runbook has the procedure. The `JWT_SECRET` half stays open (gotrue/PostgREST dual key, staging first). | The guard spec was RED on the previous-token case and is now GREEN (6/6); `make nestjs-ci` passes 533 tests. m205 is green, is RED when permission-engine's mapping is removed, and kills 5 mutants of the script. m68 is green live (9/9, static + hmac). No live rotation was run against the real `.env`. |
| N-19 | G-Net: the netseg overlay only added bridges. Every service kept the flat `mini-baas` bridge, so with the overlay on, kong could still open postgres:5432 and vault:8200. Its gate m66 had failed on line 52 since the flatten (`cd /opt/.claude`). m66 also only tested alpine stand-ins, and nothing ran it. | `feat/g-net-segmentation`: the overlay moves the engines to `net-data` and vault to `net-vault` with `!override`, and joins every engine client to them. `make up NETSEG=1` opts in; `make prod-up` includes it (`PROD_NETSEG=0` drops it). m66 is rewritten to render the real services on the dev and prod stacks and to probe a segmented live stack. It runs in CI's security-gates job, and m205 was added there as well. | m66 was RED on the old overlay (`postgres is on mini-baas,net-data`) and is now GREEN (64 edges, both stacks). It kills 6 overlay mutants: gotrue or storage-router left off net-data, kong on net-data, the `db` alias dropped, mongo or vault not overridden. Live, `PACKAGE=max NETSEG=1` gives 34/34 healthy and 13/13 targets up; kong → postgres is refused, query-router connects; m27 passes all 8 engines; m52, m68, m204 and the fast battery are green. |
| N-20 | G-Vault was listed as deferred, but its code had already shipped: migration 060, `credential_ref`, the max-tier `ErrPlaintextDsnForbidden` and the Rust `VaultProvider` resolve path. Its gate m121 was in no workflow. It also pulled `hashicorp/vault:latest`, which is now 2.1.1, while the stack runs 1.21. | `test/g-vault-nightly`: m121 is pinned to the stack's Vault (`public.ecr.aws/hashicorp/vault:1.21`), gains `M121_AR_IMAGE`/`M121_DPR_IMAGE` to reuse prebuilt images (never removed), and its "built from ?" is fixed (a pre-flatten `git -C` path). The new nightly job `vault-credref` runs it from source. The runbook and ASVS rows now say done. | m121 is green on Vault 2.1.1 and again on the pinned 1.21, the second time with the live stack's current-source images: 403 and 0 rows / 201 with the DSN resolved from Vault (200) / 201 at parity. One from-source rerun after a build-cache prune filled `/var` (42 MB free). mongo and cockroach restarted by themselves and all 34 containers are healthy again. The nightly job has not run yet (no GitHub token), so its result is **UNKNOWN**. |
| N-21 | The G-Net overlay, now the default in `make prod-up`, puts mongo and dynamodb-local on `net-data` only. The `docker run` sidecars in `vault-seed.sh`, `vault-restore.sh` and `seed-live-demo.sh` stayed on the app bridge, so they could no longer reach those engines: backups and the demo seed skipped mongo without any error. | `fix/netseg-engine-sidecars`: the new `scripts/lib/lib-netseg.sh` `engine_net` starts each sidecar on the engine's own bridge (falling back to the app bridge), and m66 gains a live check that proves it. | RED: on a segmented stack, HEAD's `vault-seed.sh` skipped mongo. GREEN: the fixed script captures it. dynamodb-local is reachable but has no tables, so skipping it is correct. m66 is green live: mongo and dynamodb-local are reachable over `net-data` and unreachable over the app bridge. |
| N-22 | adapter-registry-go's `POST /databases`, `GET /databases` and `GET /databases/{id}` take a caller-asserted `X-Baas-Tenant-Id` with no credential (only `/connect` and the deletes check the service token; `requser.go:30-33` wrongly says write paths do). Any container on the app bridge could list any tenant's mounts and register a mount with a DSN of its choice under any tenant. Reproduced live: an app-bridge sidecar with an arbitrary tenant header got 200. | `fix/netseg-registry-bridge`, devil-reviewed (PROCEED-WITH-CONDITIONS). The netseg overlay takes adapter-registry-go off the app bridge (`net-data`, `net-vault`, new `net-registry`), and kong joins `net-registry` for its `/admin/v1` route. Its other callers already share a bridge with it: query-router, data-plane-router-rust, tenant-control, schema-service and prometheus. The list was cross-checked against `.env`, code defaults, every plane file and the scripts. The prod overlay also strips its `127.0.0.1:3021` host port; no script uses it. Dev (flat base) is unchanged. | m66 was RED on the old overlay (`adapter-registry-go is on 'mini-baas,net-data,net-vault'`) and is GREEN now. It kills two mutants: the registry kept on mini-baas, and studio joined to net-registry. It asserts net-registry holds exactly {adapter-registry-go, kong}, so a service added to the app bridge alone can never reach it. Live, `PACKAGE=max NETSEG=1`: the same sidecar probe times out, while kong and query-router still connect. 34/34 healthchecks healthy, 13/13 targets up, no alert firing. m165's provisioning arm registers a mount through kong `/admin/v1`, m176 queries through the mounts (PASS), and the fast battery is green. WAF → kong and kong `/admin/v1/databases` return 200. m195 PASS (14 stripped services). |
| N-23 | Kong behind the WAF saw every client as the WAF: `trusted_ips` was unset, so Kong ignored the WAF's `X-Real-IP` and took the WAF's bridge IP. That IP is private, so ip-restriction admitted the internet, and every `limit_by: ip` counter was one shared bucket. GoTrue's limiter, keyed on the `X-Real-IP` Kong sends, was one bucket too. Worse, the WAF appended to a client's `X-Forwarded-For`, and ipguard (the tenant IP allowlist) and the audit interceptor take its first hop as the client: a forged `X-Forwarded-For` bypassed the allowlist and forged the audited IP. Reproduced by m207: a public client's `/studio` reached the upstream lookup, and the upstream saw `x-real-ip=<WAF IP>` and `x-forwarded-for=10.9.9.9, <client>, <WAF>`. | `fix/kong-trusted-ips`, devil-reviewed (PROCEED-WITH-CONDITIONS). The prod and cloud overlays set `KONG_TRUSTED_IPS` to the private ranges (`PROD_KONG_TRUSTED_IPS=` opts out); base/dev is unchanged. Trusting the WAF makes Kong forward the `X-Forwarded-*` it receives, so the WAF now overwrites `X-Forwarded-For` with `$remote_addr` and drops a client's `X-Forwarded-Host/-Port/-Path/-Prefix`. Kong then fills those in itself, the same values as before. Every seed/provision script calls Kong on `127.0.0.1:<port>`, not through the WAF, so none changes. Through the public edge, `/admin/v1` and `/studio` now refuse a public client, as intended. | New gate m207. Statically, base has no trusted IPs, prod and cloud trust the private ranges, and the opt-out works. Live, a client on a TEST-NET-3 bridge goes through the WAF image with the repo config, then Kong on a private bridge, then a header-echo upstream. `/studio` answers 403 from ip-restriction despite a forged private `X-Real-IP`, and the upstream sees the client's IP in `X-Real-IP` and first in `X-Forwarded-For`, with no forged value. A mutant WAF with the old header lines must leak `x-forwarded-host=evil.example` and the forged XFF (it does), which proves the WAF half is load-bearing. RED before the fix on all three counts. `kong check` accepts both the empty and the set value. |
| N-24 | Kong's internal-only routes relied on `ip-restriction` alone. Ten of the eleven ip-restricted routes (`/meta/v1` → pg-meta, `/admin/v1/{provision,tenants,keys,webhooks,function-triggers,function-schedules,function-secrets,migrate,rotate}`) checked only `key-auth`, which the public anon key passes; only the `/admin/v1` catch-all had `acl baas-admin`. Behind the WAF every client reaches Kong from a private bridge IP (N-23), so the allowlist let anyone through: reproduced live, an anon-key `GET /admin/v1/keys` through the public WAF on :8880 got the upstream's 404 and `/meta/v1/tables` reached Kong's upstream lookup (503: pg-meta is not defined in this repo). The upstreams' own service token was the only barrier left. Found by the devil review of N-23. | `fix/kong-admin-acl`. The ten routes gain `acl allow: [baas-admin]`, i.e. the service_role key only. Every in-repo caller already uses that key: the SDK admin, webhooks and functions clients, m56 and the seed scripts. Studio reaches pg-meta directly, not through Kong. `/studio` is a browser UI that cannot send an apikey header, so it keeps ip-restriction alone until N-23 makes that allowlist see the real client. | New gate m206 (no stack) runs a throwaway Kong on the repo kong.yml, reads Kong's own parsed routes and plugins, and requires `acl baas-admin` on every ip-restricted route but studio. It then probes each route from 127.0.0.1: the anon key must get the acl's 403 and the service key must pass. It was RED on the old config (`admin-function-schedules ... is ip-restricted but not acl baas-admin`) and is GREEN with 11 routes. Live, after a kong restart: the anon key through the WAF gets 403 on every admin route, and the service key still reaches the upstreams. m56, m157, m176, m195 and m197 PASS. |

## Open, tracked

| ID | Finding | Note |
|---|---|---|
| M-5 | `sub` not validated as a UUID | **Rejected as asked**: SSO sessions carry the IdP's raw subject and passkeys' `user_id` is client-supplied — a UUID check breaks both (and gate m64). The real hole it pointed at is closed instead: with an empty `GOTRUE_JWT_ISSUER` the verifier accepted any same-secret token (cross-app, seed) as a user session; tenant-control now refuses to start with an empty issuer (`fix/sec-m5-issuer-guard`, `JWT_ALLOW_NO_ISSUER=1` opts out; `TestRequireIssuer`). |
| N-22 (rest) | adapter-registry-go still trusts the asserted tenant on register/list/findOne from any `net-data` or `net-vault` member and from kong. A compromised backend on net-data (grafana is published on :3030) could register a mount whose DSN points into net-vault or the app bridge, and data-plane-router-rust would dial it on `/connect`: SSRF. | **Owner decision.** The fix is to require the service token (or `ADAPTER_REGISTRY_IDENTITY_HMAC`) on those routes too. About 20 seed/provision scripts and any external `/admin/v1/databases` consumer send only the service_role key, and in the default `hmac` token mode kong cannot add a valid per-request signature. The false `requser.go` comment is corrected with that change: editing it alone would mark all six Go images stale for a comment. |
| N-23 (rest) | Kong trusts `X-Real-IP` from any private peer. The WAF sees a private gateway IP for a client that comes through docker's userland proxy (IPv6 on a published port) or through any proxy put in front of the WAF, so that client passes ip-restriction and shares a rate bucket. Jailed function Workers reach Kong through functions-relay (private) and can pick their own rate-limit key. | Owner decision: bind the WAF's published ports to IPv4 or enable `ip6tables`, and add `real_ip` in the WAF behind any front proxy; or pin trust to the WAF's own address. With N-24, the admin routes also need the service key, so ip-restriction is no longer their only barrier. |
| — | Kong's per-IP limit on `/auth/v1` is 60000/min, so it protects nothing against credential stuffing. GoTrue's own limiter is the real control, and after N-23 it counts per client. | Owner decision on a Kong-side login limit (per-route numbers affect real users). |
| — | m165's static check "app name leaked into grobase src/" fails on develop: the vault42 scope-key / RBAC code in `src/control-plane` (`pubkeys`, `teams/grants.go`, `environments`, `github`) names vault42. It predates N-22. | Owner decision: narrow the grep to app-specific logic, or rename the vault42-named feature code. |
| — | `edition-query` offer build was intermittent in CI | Cause found: concurrent Rust builds raced on a `sharing=shared` cargo cache mount ('failed to unpack package'); now `sharing=locked`. |

## False positive, mitigated or by design

C-4 (`VAULT_API_ADDR` is the advertise address) · C-6 (dev-only profile, localhost) · H-2/H-3
(container-internal binds) · H-7 (verify needs the service token; fast hash for new keys) · H-8
(`GITHUB_TOKEN` is per-job and masked; H-9 least privilege done in `a51ffad2`) · H-10 (the original
`sh -c` expands inside the container and is not injectable; the reverted fix broke backups) · H-12
(`:?` in base breaks every render without `.env`; enforced by the preflight, m194, which `make prod-up` runs before compose up) · H-17 (realtime
already refuses NoAuth under max) · H-18 (`SMTP_SECURE=false` still upgrades via STARTTLS) · H-21
(2026 CVE ids are real) · L-1 (`NODE_ENV=production` in the image) · L-2 (algorithm pinned before
parse) · L-3 (`|| true` on `vault status` is required) · L-7 (applies to push, not automations) ·
L-8 (masked) · L-13 (fast hash + upgrade) · L-14 (`chmod 600` everywhere) · M-2 (hash comparison
is length-independent) · M-6 (deep operator check exists and is tested; now also property-tested: `collections.filter.property.spec.ts`, 6000 seeded random filters with duplicate/`__proto__`/dotted/operator keys against an independent oracle — refused exactly when unsafe, an accepted one always keeps the caller's `owner_id`; kills mutants that skip dotted keys, arrays, recursion, the owner_id field check or both checks, or let `__proto__` reach the driver; a 20000-deep filter answers 400, never a 500) · M-7 (session service
retired) · M-13 (already parametrized: `auth-api.yml:48` is `${GOTRUE_PASSWORD_MIN_LENGTH:-8}`, the
prod overlay pins 12 and m195 asserts both the 12 and the 8) · M-14/M-15 (single replica; Kong
limits) · M-18 (mailpit is dev) · L-5 (accepted) · M-10 (default usernames: the preflight already refuses the `mongo` and `minioadmin` root usernames as dev defaults, m194; the Postgres superuser stays `postgres` because the supabase image's roles and ~110 scripts use it — its password is enforced by the preflight and the prod overlay publishes no Postgres port, m195 `static_ports`; a username is not a secret). Also planned but found to be a non-finding:
m5/m60's references to `.github/workflows/mini-baas-security.yml` are not dead — that workflow
exists and is the blocking `security-gate`; the "missing file" was an artifact of the pre-rebuild
`develop`.

## Where CI proves it

- **CI** (`ci.yml`, push to main and develop): unit suites incl. functions-runtime Deno; security
  gates m195 m196 m198 m199; integration runs m157 m197 m200 m201 m202 m203 plus the live test phases; cloud
  gates incl. m83; m194 and m52 (alert rules + promtool unit tests) in the lint job; `make prettiers-check` (every language's formatter, pinned) in
  the formatters job.
- **Security** (`mini-baas-security.yml`): gitleaks · trufflehog · semgrep · trivy · cargo-audit +
  govulncheck · ZAP baseline → `security-gate`.
